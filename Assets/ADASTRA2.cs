using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ADASTRA2 : MonoBehaviour
{
    public float DEMO_layeredIntensityStems = 1.0f;
    public float DEMO_blendAcousticElectic = 0.0f;
    public bool DEMO_useAlternateEffectsStack = false;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
